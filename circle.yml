machine:
  node:
    version: 7.7.2
dependencies:
  override:
    - npm install -g npm@next
    - npm install -g istanbul
    - npm install -g yarn
    - yarn install
    - npm install --only=dev
general:
  artifacts:
    - ./coverage
